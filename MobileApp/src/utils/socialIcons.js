/**
 * Ionicons logo name per platform, keyed the same as the `social_links` entries the API
 * sends (DeveloperResource, PartnerResource) — one map so every screen that renders a
 * social link list looks the same instead of picking its own icon per platform.
 */
export const SOCIAL_ICONS = {
  instagram: 'logo-instagram',
  facebook: 'logo-facebook',
  youtube: 'logo-youtube',
  twitter: 'logo-twitter',
  linkedin: 'logo-linkedin',
};
